{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "title": "Ability",
    "description": "An ability of unit",
    "type": "object",
    "properties": {
        "type": {
            "description": "Ability type",
            "type": "string"
        },
        "usage": {
            "description": "Ability usage policy",
            "type": "string"
        }
    },
    "required": ["type", "usage"]
}